AI Models Used:
- GPT-OSS 120B (Medium) – version 1.0.0 (used in earlier sessions)
- Gemini 3.5 Flash (High) (used in the current session)

Prompts Used & Tasks Executed:
1. Pipeline Fix Prompt:
   - "You are modifying an existing Python RAG pipeline. This is an existing project, not a new implementation. Your task is to FIX ONLY the process_reports() function in pipeline.py..."
   - Action: Resolved SyntaxError, nested duplicate loops, and missing try/except block. Refactored the code to accumulate parent and child chunks in memory across multiple PDF files and write them to disk at the end of the loop, preventing files from being overwritten.
   
2. Logical Review/Verification Prompt:
   - "You are performing a code review of an existing Python RAG pipeline. Your task is NOT to rewrite the code. Your task is ONLY to verify whether the parent-child hierarchical chunking implementation works correctly..."
   - Action: Analyzed the complete flow (extraction, structure, representation, child/parent chunk generation, linking, accumulation, embeddings, FAISS indexing, and hierarchical search). Identified a minor edge case where pages containing only tables/images without text elements would not produce child chunks.

3. AI Documentation Prompt:
   - "now in the documentation folder create a text file named ai_usage.txt 2 and give info abt these r AI usage details there. These might be names of the models used (with version), prompts used, etc."
   - Action: Documented the models, prompts, and tasks.

Notes:
- The AI models were used as coding partners for debugging, code review, and writing documentation.
- All code changes and analyses were validated using unit tests (`pytest`) and local execution (`run_rag_pipeline.py`).
